# macOS 迁移手工回归清单

## 背景

- 日期：2026-03-23
- 目标：对 ToolsBox macOS 迁移后的关键能力做回归检查
- 说明：本清单分为“已在当前环境验证”和“需要桌面手工验证”

## 已在当前环境验证

- `npm run build`
  - 结果：通过
- `npm run typecheck`
  - 结果：通过
- `npm run lint`
  - 结果：通过
- `npm test`
  - 结果：通过
- `npm run pack`
  - 结果：通过，产物目录可生成
  - 备注：仍为 ad-hoc 签名，未 notarize，只能说明打包链路正常

## 已做针对性修补

- 分类折叠状态持久化
  - 之前问题：顶级分类折叠只存在组件内存里，重启后丢失
  - 当前处理：侧边栏折叠时同步调用 `saveCategory`，将 `collapsed` 写回数据层

## 需要桌面手工验证

### 窗口与托盘

- 启动应用，确认主窗口能正常显示
- 点击关闭按钮
  - 预期：若“最小化到托盘”开启，窗口隐藏而非退出
- 点击托盘图标
  - 预期：窗口在显示 / 隐藏之间切换
- 托盘菜单点“退出”
  - 预期：应用真正退出

### 主题与外观

- 设置里切换“跟随系统 / 深色 / 浅色”
  - 预期：主题即时变化，重启后保持
- 设置里切换“网格 / 列表”
  - 预期：主内容区即时切换，重启后保持
- 拖动侧边栏宽度
  - 预期：宽度即时变化，重启后保持

### 分类

- 新建顶级分类
  - 预期：保存成功并出现在侧边栏
- 在顶级分类下新建子分类
  - 预期：树结构显示正常
- 折叠 / 展开顶级分类
  - 预期：状态即时切换，重启后保持
- 点击父分类
  - 预期：父分类自身工具和其子分类工具都能显示
- 删除父分类
  - 预期：子分类一起删除，相关工具移到 `misc`

### 工具新增与编辑

- 新建 `app`、`shell`、`python`、`jar`、`url` 五类工具
  - 预期：都能正常保存
- 给 `shell` / `python` / `jar` 工具设置工作目录
  - 预期：保存成功，编辑后能回显
- 在工具弹窗中拖入 `.app` 或脚本文件
  - 预期：名称、路径、类型自动填充；可取到图标时显示图标
- 给工具选择颜色标签
  - 预期：卡片 / 列表行能显示颜色标识

### 启动行为

- 启动 `url`
  - 预期：无协议时自动补 `https://`
- 启动 `app`
  - 预期：能用 `open ... --args` 正常启动
- 启动 `shell` / `python` / `jar`
  - 预期：参数拆分正常，工作目录生效
- 右键“在终端中打开”
  - 预期：Terminal 打开到目标目录
- 右键“在访达中显示”
  - 预期：Finder 选中目标文件

### 数据迁移

- 用 `npm run migrate:lily -- /path/to/data.json`
  - 预期：生成 `toolsbox-data.json`
- 导入旧数据后检查：
  - 预期：Windows 专有类型被跳过，颜色映射到 `accentColor`，两级分类结构正常

## 当前结论

- 代码层、构建层、测试层、打包层未发现新的阻断问题
- 剩余风险主要在 Electron 原生交互和桌面集成，需要按上面的手工项做一次真实点击验证
