# 继续任务

## 2026-03-19

- 补完 Phase 3 的“快速创建快捷方式”
- 工具右键菜单新增“创建桌面快捷方式”
- URL 生成 `.webloc`
- 本地工具生成 `.command` 并自动赋予可执行权限
- 创建后自动在访达中定位结果文件
- 补充 `tests/shortcuts.test.js` 覆盖快捷方式生成逻辑
- 补完 Phase 4 的“全局唤起面板”
- 默认全局快捷键为 `Cmd/Ctrl + Shift + K`
- 设置中新增全局唤起面板开关
- 补完 Phase 5 的 `lint / typecheck`
- 新增 `eslint.config.mjs`
- 新增 `tsconfig.renderer.json`
- 为 README 补充检查命令说明
- 增加单实例互斥，重复启动时只唤起已有窗口
- 修复托盘退出无效，真正退出时不再被最小化到顶栏
- 统一应用显示名称为 `LaunchBox`，避免开发态继续显示 `Electron`
- 新增应用图标资源：`icon.png`、`icon.icns`、`icon.ico`
- 重做状态栏图标：`tray-icon.png`、`tray-icon@2x.png`
- 右上角工具栏新增显示密度切换：小 / 中 / 大
- 小模式改为更紧凑的无背景排布，便于一屏展示更多工具
- 工具卡片改为单击直接启动
- 移除大小模式下所有悬浮启动图标
- 新增“多选”显式模式，作为 `Cmd/Ctrl`、`Shift` 之外的另一种批量选择方式
- 排序入口图标改为更明确的 `⇅`
- 增加“自定义排序”选项，可拖拽调整顺序
- 切换回自定义排序时，拖拽顺序不会丢失
- 自定义排序支持直接拖动工具图标调整位置
- 修复自定义排序拖拽误触发导入遮罩的问题
- 导入拖拽遮罩只覆盖内容区，不再盖到标题栏
- 补齐拖拽启动数据，修复图标看似可拖但实际拖不动的问题
- 自定义排序改为按住拖排，不再依赖不稳定的原生拖拽
- 默认排序切到自定义，并增加明显的“拖排中”状态提示
- 修复拖排目标卡不响应的问题，拖到其他卡片上会立即预排
- 拖排命中改为全局指针定位，不再依赖卡片自身 hover 事件
- macOS 菜单栏显式改为 LaunchBox，避免继续显示 Electron
- 标题栏改为更接近 macOS unified toolbar 的轻量样式
- 设置页新增“全局快捷键”输入框，支持自定义快速启动器唤起键
- 自定义快捷键保存后会同步刷新菜单栏、托盘菜单和全局注册
- 自定义快捷键输入无效时自动回退默认 `CommandOrControl+Shift+K`
- 工具图标支持右键直接打开编辑详情，减少一次右键菜单跳转
- 补充 `tests/store.test.js` 覆盖自定义快捷键清洗逻辑

## 2026-03-23

- 按远端 macOS 迁移方案完成主干迁移，数据模型切到 `workingDirectory`、`accentColor`、两级分类、`viewMode`、`sidebarWidth`
- 主进程移除应用库、目录导入、快捷方式、全局唤起等旧能力，启动链路改为 macOS 专用实现
- `openInTerminal` 改为 `osascript` stdin，`showInFinder` 改为 `execFile`，避免命令拼接风险
- 设置页切到系统 / 深色 / 浅色主题和网格 / 列表视图配置
- 侧边栏改成两级分类树，支持拖拽调整宽度并静默持久化
- 工具弹窗新增工作目录、颜色标签、拖放文件自动识别和图标提取
- 删除旧的 AppLibrary / QuickLauncher / imports / shortcuts 相关代码和测试
- 新增 `scripts/import-lily.mjs`，用于 Lily 数据迁移
- 修复迁移方案里的两个已知问题：父分类现在可以直接筛选，侧边栏宽度重启后能恢复
- 继续补了一轮回归：补上分类折叠状态持久化
- 新增 `notes/macOS-迁移手工回归清单.md`
- 更新 `README.md` 与 `notes/远端更新审计.md`
- 校验通过：`npm run build`、`npm run typecheck`、`npm run lint`、`npm test`
