# 审计记录 2026-03-23（第四轮）

## 本轮触发提交

- `cd030b1` — fix: keep main and sub categories in separate panes
- `fa8bda7` — Merge remote-tracking branch

## 变更文件

- `src/renderer/components/Sidebar.tsx` — 大幅精简，严格分离主/副分栏职责

## 审计结论

### 主/副分栏严格分离（已核实）

本次修复彻底落地「主分栏只在顶部 Tab，副分栏只在左侧 Sidebar」的设计约束。

Sidebar.tsx 变更要点：
1. **删除主分类入口**：Sidebar 不再渲染任何顶级分类（`parentId` 为空的分类），这些分类仅出现在 MainContent 顶部 Tab 栏
2. **纯副分栏展示**：只展示当前激活主分类（`activeTopCategoryId`）的子分类列表
3. **无主分类选中时的提示**：当 `selectedCategoryId === 'all'`（或无效值），`activeTopCategory` 为 null，Sidebar 显示"先从顶部选择一个大分类"引导文字
4. **删除 collapsed 状态**：`Category.collapsed` 字段和 `saveCategorySilent` 调用均已移除，Sidebar 不再有折叠/展开逻辑
5. **删除最近使用**：完全无 recentTools 相关代码（上一轮已确认，本次保持）

### 代码质量核实

- `hoverTimerRef` useEffect cleanup（第 25-29 行）：**已存在**，Sidebar 在卸载时会清除 timer，不会有 stale timer fire
- `categoryChildren` 用 `useMemo` 缓存（Map），`getCount` 用 BFS，逻辑正确
- `activeTopCategoryId` 计算：选中子分类时取 `parentId`，选中主分类时取 `id`，选中 all 时为 null — 逻辑正确
- 拖拽宽度：仍用 `saveSettingsSilent`（正确，不弹 Toast）

### 遗留问题确认

- **MainContent `hoverTimerRef` 缺少 useEffect cleanup** 仍存在（Codex 尚未处理）：组件卸载时 timer 可能 fire，但仅触发一次无效的 `selectCategory`，低风险

### CODEX.md 同步更新

- 九章「分类系统」：更新 Sidebar 说明，明确当前行为（纯副分栏，无主分类，无折叠）
- 十二章「当前进展」：`cd030b1` 修复记录为已完成

## 当前状态

无阻断级问题。主/副分栏职责边界清晰，代码与设计意图对齐。
